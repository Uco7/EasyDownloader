<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Download Result</title>
</head>
<body>
  <form action="/download" method="post">
    <input type="text" name="videoUrl" placeholder="Enter YouTube URL" required />
    <button type="submit">Download</button>
  </form>

  <% if (error) { %>
    <p style="color: red;"><%= error %></p>
  <% } %>

  <% if (downloadedFileName) { %>
    <p><strong>Downloaded File:</strong> <%= downloadedFileName %></p>
    <video controls width="400" src="/downloads/<%= encodeURIComponent(downloadedFileName) %>"></video><br />
    <a href="/downloads/<%= encodeURIComponent(downloadedFileName) %>" download>⬇️ Download video</a><br />
  <% } %>
</body>
</html>
